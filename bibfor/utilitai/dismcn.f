      SUBROUTINE DISMCN(QUESTI,NOMOBZ,REPI,REPKZ,IERD)
C            CONFIGURATION MANAGEMENT OF EDF VERSION
C MODIF UTILITAI  DATE 03/07/2012   AUTEUR PELLET J.PELLET 
C ======================================================================
C COPYRIGHT (C) 1991 - 2012  EDF R&D                  WWW.CODE-ASTER.ORG
C THIS PROGRAM IS FREE SOFTWARE; YOU CAN REDISTRIBUTE IT AND/OR MODIFY
C IT UNDER THE TERMS OF THE GNU GENERAL PUBLIC LICENSE AS PUBLISHED BY
C THE FREE SOFTWARE FOUNDATION; EITHER VERSION 2 OF THE LICENSE, OR
C (AT YOUR OPTION) ANY LATER VERSION.
C
C THIS PROGRAM IS DISTRIBUTED IN THE HOPE THAT IT WILL BE USEFUL, BUT
C WITHOUT ANY WARRANTY; WITHOUT EVEN THE IMPLIED WARRANTY OF
C MERCHANTABILITY OR FITNESS FOR A PARTICULAR PURPOSE. SEE THE GNU
C GENERAL PUBLIC LICENSE FOR MORE DETAILS.
C
C YOU SHOULD HAVE RECEIVED A COPY OF THE GNU GENERAL PUBLIC LICENSE
C ALONG WITH THIS PROGRAM; IF NOT, WRITE TO EDF R&D CODE_ASTER,
C    1 AVENUE DU GENERAL DE GAULLE, 92141 CLAMART CEDEX, FRANCE.
C ======================================================================
      IMPLICIT NONE
C     --     DISMOI(CHAM_NO)
C     ARGUMENTS:
C     ----------
      INCLUDE 'jeveux.h'
      INTEGER REPI,IERD
      CHARACTER*(*) QUESTI
      CHARACTER*(*) NOMOBZ,REPKZ
      CHARACTER*24 QUESTL
      CHARACTER*32 REPK
      CHARACTER*19 NOMOB
C ----------------------------------------------------------------------
C     IN:
C       QUESTI : TEXTE PRECISANT LA QUESTION POSEE
C       NOMOB  : NOM D'UN OBJET DE TYPE NUM_DDL
C     OUT:
C       REPI   : REPONSE ( SI ENTIERE )
C       REPK   : REPONSE ( SI CHAINE DE CARACTERES )
C       IERD   : CODE RETOUR (0--> OK, 1 --> PB)
C
C ----------------------------------------------------------------------
C     VARIABLES LOCALES:
C     ------------------
      CHARACTER*8 NOGD
      CHARACTER*8 K8BID
C
C
C
C-----------------------------------------------------------------------
      INTEGER IADESC ,IAREFE 
C-----------------------------------------------------------------------
      CALL JEMARQ()
      NOMOB = NOMOBZ
      REPK = ' '
      QUESTL = QUESTI
      IF (QUESTI.EQ.'NB_EQUA') THEN
         CALL JELIRA(NOMOB//'.VALE','LONMAX',REPI,K8BID)
      ELSE IF (QUESTI.EQ.'NOM_MAILLA') THEN
         CALL JEVEUO(NOMOB//'.REFE','L',IAREFE)
         REPK = ZK24(IAREFE-1+1) (1:8)
      ELSE IF (QUESTI.EQ.'NB_DDLACT') THEN
         CALL JEVEUO(NOMOB//'.REFE','L',IAREFE)
         CALL DISMPN(QUESTI,ZK24(IAREFE-1+2)(1:8)//'.NUME      '
     &               ,REPI,REPK,IERD)
      ELSE IF (QUESTI.EQ.'TYPE_CHAMP') THEN
         REPK = 'NOEU'
      ELSE IF (QUESTL(1:7).EQ.'NUM_GD ') THEN
         CALL JEVEUO(NOMOB//'.DESC','L',IADESC)
         REPI = ZI(IADESC)
      ELSE IF (QUESTL(1:7).EQ.'NOM_GD ') THEN
         CALL JEVEUO(NOMOB//'.DESC','L',IADESC)
         CALL JENUNO(JEXNUM('&CATA.GD.NOMGD',ZI(IADESC)),REPK)
      ELSE IF (QUESTI.EQ.'TYPE_SUPERVIS') THEN
         CALL JEVEUO(NOMOB//'.DESC','L',IADESC)
         CALL JENUNO(JEXNUM('&CATA.GD.NOMGD',ZI(IADESC)),NOGD)
         REPK='CHAM_NO_'//NOGD
      ELSE IF (QUESTI.EQ.'PROF_CHNO' ) THEN
         CALL JEVEUO(NOMOB//'.REFE','L',IAREFE)
         REPK = ZK24(IAREFE+1)
      ELSE IF (QUESTI.EQ.'NOM_NUME_DDL' ) THEN
         CALL ASSERT(.FALSE.)
         CALL JEVEUO(NOMOB//'.REFE','L',IAREFE)
         REPK = ZK24(IAREFE+1)
C        ATTENTION : ZK24(IAREFE+1) EST UN PROF_CHNO (K19)
C        EN TRONQUANT A K14, ON NE TROUVE PAS TOUJOURS DE NUME_DDL
C        CE DISMOI EST DANGEREUX ET NE DEVRAIT PAS ETRE UTILISE

      ELSE
         IERD=1
      END IF
C
      REPKZ = REPK
      CALL JEDEMA()
      END
