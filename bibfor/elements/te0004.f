      SUBROUTINE TE0004 (OPTION,NOMTE)
      IMPLICIT NONE
      CHARACTER*16 OPTION,NOMTE
C ----------------------------------------------------------------------
C            CONFIGURATION MANAGEMENT OF EDF VERSION
C MODIF ELEMENTS  DATE 11/06/2012   AUTEUR DELMAS J.DELMAS 
C ======================================================================
C COPYRIGHT (C) 1991 - 2012  EDF R&D                  WWW.CODE-ASTER.ORG
C THIS PROGRAM IS FREE SOFTWARE; YOU CAN REDISTRIBUTE IT AND/OR MODIFY
C IT UNDER THE TERMS OF THE GNU GENERAL PUBLIC LICENSE AS PUBLISHED BY
C THE FREE SOFTWARE FOUNDATION; EITHER VERSION 2 OF THE LICENSE, OR
C (AT YOUR OPTION) ANY LATER VERSION.
C
C THIS PROGRAM IS DISTRIBUTED IN THE HOPE THAT IT WILL BE USEFUL, BUT
C WITHOUT ANY WARRANTY; WITHOUT EVEN THE IMPLIED WARRANTY OF
C MERCHANTABILITY OR FITNESS FOR A PARTICULAR PURPOSE. SEE THE GNU
C GENERAL PUBLIC LICENSE FOR MORE DETAILS.
C
C YOU SHOULD HAVE RECEIVED A COPY OF THE GNU GENERAL PUBLIC LICENSE
C ALONG WITH THIS PROGRAM; IF NOT, WRITE TO EDF R&D CODE_ASTER,
C   1 AVENUE DU GENERAL DE GAULLE, 92141 CLAMART CEDEX, FRANCE.
C ======================================================================
C RESPONSABLE DELMAS J.DELMAS
C
C     BUT:
C       !! ROUTINE GENERIQUE !!
C       ASSURER LE PASSAGE ELGA -> ELNO
C
C.......................................................................
C
C --------- DEBUT DECLARATIONS NORMALISEES  JEVEUX ---------------------
C
      INTEGER  ZI
      COMMON  / IVARJE / ZI(1)
      REAL*8             ZR
      COMMON  / RVARJE / ZR(1)
      COMPLEX*16         ZC
      COMMON  / CVARJE / ZC(1)
      LOGICAL            ZL
      COMMON  / LVARJE / ZL(1)
      CHARACTER*8 ZK8
      CHARACTER*16 ZK16
      CHARACTER*24 ZK24
      CHARACTER*32 ZK32
      CHARACTER*80 ZK80
      COMMON /KVARJE/ZK8(1),ZK16(1),ZK24(1),ZK32(1),ZK80(1)
C
C --------- FIN  DECLARATIONS  NORMALISEES  JEVEUX ---------------------
C
      INTEGER NDIM,NNO,NNOS,NPG,NBSP
      INTEGER IPOIDS,IVF,IDFDE,JGANO
      INTEGER IRET,NBCMP,ITABIN(7),ITABOU(7)
      INTEGER IINPG,IOUTNO

      CHARACTER*4 FAMI
C
C ----------------------------------------------------------------------
C
      IF (OPTION.EQ.'DERA_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDERAPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDERANO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'DISS_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDISSPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDISSNO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EFGE_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PEFGAR'  ,7,ITABIN,IRET)
        CALL TECACH('OOO','PEFFORR' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'ENDO_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PTRIAPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PTRIANO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPFD_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPFP_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPME_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPMG_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPSG_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPSI_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPSP_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'EPVC_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PDEFOPG' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PDEFONO' ,7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'SIGM_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PCONTRR' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PSIEFNOR',7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'SIEF_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PCONTRR' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PSIEFNOR',7,ITABOU,IRET)

      ELSEIF (OPTION.EQ.'VARI_ELNO') THEN
        FAMI = 'RIGI'
        CALL TECACH('OOO','PVARIGR' ,7,ITABIN,IRET)
        CALL TECACH('OOO','PVARINR' ,7,ITABOU,IRET)

      ELSE
        CALL ASSERT(.FALSE.)
      ENDIF
C
C    -------------------------------------------------------------------
C    -- COMMUN A TOUTES LES OPTIONS
C    -------------------------------------------------------------------
C
C --- IL FAUDRAIT PENSER A RECUPERER FAMI DE MANIERE AUTOMATIQUE ET SURE
      CALL ELREF4(' ',FAMI,NDIM,NNO,NNOS,NPG,IPOIDS,IVF,IDFDE,JGANO)
      IINPG=ITABIN(1)
      IOUTNO=ITABOU(1)

C --- NOMBRE DE COMPOSANTE
      NBCMP=ITABIN(2)/ITABIN(3)
      CALL ASSERT(NBCMP.GT.0)
      CALL ASSERT(NBCMP.EQ.ITABOU(2)/ITABOU(3))

C --- ON ESSAIE UNE AUTRE FAMILLE DE POINT DE GAUSS
      IF (ITABIN(3).NE.NPG) THEN
        FAMI='MASS'
        CALL ELREF4(' ',FAMI,NDIM,NNO,NNOS,NPG,IPOIDS,IVF,IDFDE,JGANO)
      ENDIF
      CALL ASSERT(ITABIN(3).EQ.NPG)

C --- NOMBRE DE SOUS-POINT
      NBSP=ITABIN(7)
      CALL ASSERT(NBSP.EQ.ITABOU(7))

C --- NOMBRE DE COMPOSANTE DYNAMIQUE
      IF (OPTION.EQ.'VARI_ELNO') THEN
        NBCMP=ITABIN(6)
        CALL ASSERT(NBCMP.EQ.ITABOU(6))
      ENDIF

      CALL PPGAN2(JGANO,NBSP,NBCMP,ZR(IINPG),ZR(IOUTNO))

      END
